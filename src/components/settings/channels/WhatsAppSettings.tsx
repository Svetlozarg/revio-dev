import WAConnectButton from '@/components/global/WAConnectButton';
import { Box, Typography } from '@mui/material';

export default function WhatsAppSettings() {
  return (
    <Box>
      <WAConnectButton />
    </Box>
  );
}
